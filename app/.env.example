# URL base da API backend (sem barra final)
# Dev local:  http://localhost:5000/api
# Produção:   https://<url-do-backend>/api  (substituir pelo URL real do deploy)
VITE_API_BASE=http://localhost:5000/api
